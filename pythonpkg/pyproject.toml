[build-system]
requires = [ 
    "setuptools>=42",
    "pybind11>=2.9.0",
    "cmake>=3.22",
    "scikit-build>=0.15.0",
    "ninja"
]
build-backend = "setuptools.build_meta"

[tool.pytest.ini_options]
testpaths = ["integration/python/tests/python"]
addopts = [
    "--import-mode=importlib",
]
